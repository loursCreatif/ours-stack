# Preset: `minimal` — Technical Manual

**Shell:** `<body class="preset-minimal">` · `.layout.layout-minimal` sidebar 200px  
**Nav:** `ui-monospace`, tiny uppercase `h2`, no hover pills  
**Hero:** flat band — `border-top` + `border-bottom` only, no gradient, no shadow  
**Article:** `border-radius: 0`, `box-shadow: none`  
**Prose:** `strong` underlined; `h2` uppercase mono  
**Figures:** `border: 1px solid`, monochrome SVG preferred  
**SVG grammar:** `fill: none` or white fill; `stroke: #18181b`; `stroke-width: 1`; callout numbers not color blocks

## SVG palette

`#18181b` `#3f3f46` `#166534` `#a16207` `#71717a` `#f4f4f5`

## Optional (Pico / Gwern)

Links: `text-underline-offset: .15em` on `.prose-beat a`. SVG: prefer `fill="none"` + `stroke` monochrome.

## Full CSS

Reference: `research/robots-assemblage-structurel-chantier/report-minimal.html` `<style>` block.